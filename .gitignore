# 비밀정보 — 저장소가 public이므로 커밋되면 즉시 노출된다
.env
.env.*
!.env.example
*token*.txt
*secret*

__pycache__/
*.pyc
*.docx
*.pptx
*.pdf
*.jpg
*.jpeg
*.png

# 세션 인계용 내부 점검 문서 (로컬 전용). 학생이 보는 저장소에는 올리지 않는다.
HANDOFF.md

# 주차별 누적본 제작용 작업 문서 (로컬 전용). 어느 주차에 무엇을 빼고 넣는지가 적혀 있어
# 학생이 보면 앞질러 가게 되므로 올리지 않는다.
WEEKLY_BUILD.md
